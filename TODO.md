1. use opencv or some similar library to find the elixir bar by calculating the amount filled from the left in purple in a predefined bounding box
2. generate a rudimentary reward function using the object detection, how much enemy troops are alive, how many towers i have, how many towers enemy has
3. use opencv and bar progress detection to calculate the health of each tower use predetermined bounding box, and then factor this into the reward calculation

AFTER EACH CHANGE, make a commit summarizing the changes made
