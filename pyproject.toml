[project]
name = "clash-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "adb-pywrapper>=1.3.0",
    "opencv-python",
    "pillow",
    "torch",
    "ultralytics",
    "python-dotenv",
    "roboflow",
]
